/**
 * Admin sayfaları için hesaplama sayfalarındaki zerafetle uyumlu stil sabitleri.
 * Kompakt tipografi, ince butonlar, sıkı padding.
 */

/** Sayfa başlığı – büyük ama abartısız */
export const pageTitleCls =
  "text-lg font-semibold text-gray-900 dark:text-gray-100 tracking-tight";

/** Sayfa alt başlığı / açıklama */
export const pageSubtitleCls =
  "text-xs text-gray-500 dark:text-gray-400 mt-0.5";

/** Kart başlığı (CardTitle) */
export const cardTitleCls =
  "text-sm font-semibold text-gray-800 dark:text-gray-200";

/** Kart açıklaması (CardDescription) */
export const cardDescriptionCls =
  "text-xs text-gray-500 dark:text-gray-400";

/** İstatistik kartı – değer */
export const statValueCls =
  "text-lg font-semibold text-gray-900 dark:text-gray-100 tabular-nums";

/** İstatistik kartı – etiket */
export const statLabelCls =
  "text-xs text-gray-500 dark:text-gray-400";

/** Kompakt input (hesaplama sayfalarındaki gibi) */
export const inputCls =
  "w-full min-w-0 px-2.5 py-1.5 text-sm rounded border border-gray-200 dark:border-gray-600 bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100 focus:outline-none focus:ring-1 focus:ring-indigo-500 focus:border-transparent";

/** Kompakt select */
export const selectCls =
  "px-2.5 py-1.5 text-sm rounded border border-gray-200 dark:border-gray-600 bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100 focus:outline-none focus:ring-1 focus:ring-indigo-500";

/** Tablo hücre – birincil metin */
export const tableCellCls =
  "py-2 px-3 text-sm text-gray-900 dark:text-gray-100";

/** Tablo hücre – ikincil metin */
export const tableCellMutedCls =
  "py-2 px-3 text-xs text-gray-600 dark:text-gray-400";

/** Tablo başlık */
export const tableHeadCls =
  "py-2 px-3 text-left text-xs font-medium text-gray-600 dark:text-gray-400 uppercase tracking-wider";

/** Tablo başlık – küçük, tek satır */
export const tableHeadCompactCls =
  "py-2 px-3 text-[10px] font-medium text-gray-600 dark:text-gray-400 uppercase tracking-wider whitespace-nowrap";

/** Badge küçük */
export const badgeCls =
  "text-[10px] font-medium px-1.5 py-0 rounded";

/** Admin sayfa dış wrapper – sıkı boşluk */
export const pageWrapCls =
  "space-y-4";

/** Kart içi sıkı padding */
export const cardContentTightCls =
  "p-4";
